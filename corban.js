// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});
// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW1");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});


// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW2");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});


// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW3");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});


// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW4");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});


// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW5");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW6");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW7");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW8");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW9");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW10");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW11");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW12");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW13");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW14");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("ORDER NOW15");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("Item successfully ordered"); // simple prompt

  } );


});

function order() {
  // Prompt for quantity
  let quantity = prompt("Enter the quantity:");

  // Prompt for delivery location
  let location = prompt("Enter the delivery location:");

  // Display confirmation message
  alert("Your order of " + quantity + " items has been placed for delivery to " + location);
}




function order() {
  // Prompt for quantity
  let quantity = prompt("Enter the quantity:");

  // Prompt for delivery location
  let location = prompt("Enter the delivery location:");

  // Display confirmation message
  alert("Your order of " + quantity + " items has been placed for delivery to " + location);
}

function book() {
  // Prompt for quantity
  let quantity = prompt("Enter the date of the shoot:");

  // Prompt for delivery location
  let location = prompt("Enter location of the shoot:");

  // Display confirmation message
  alert("The day of the shoot "  + quantity + " located at  "  + location  +  " has been booked succesfuly");
}


// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("BOOK NOW1");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("SUCCESSFULLY BOOKED"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("BOOK NOW2");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("SUCCESSFULLY BOOKED"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("BOOK NOW3");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("SUCCESSFULLY BOOKED"); // simple prompt

  } );


});


// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("BOOK NOW4");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("SUCCESSFULLY BOOKED"); // simple prompt

  } );


});

// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
  // Get the button element
  const button = document.getElementById("BOOK NOW5");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("SUCCESSFULLY BOOKED"); // simple prompt

  } );


});


// wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded"), function() {
  // Get the button element
  const button = document.getElementById("BOOK NOW6");
  // Add a click event listener to the button 
  button.addEventListener("click", function() {
// show a prompt or message when the button is clicked 
alert("SUCCESSFULLY BOOKED"); // simple prompt

  } );


}
